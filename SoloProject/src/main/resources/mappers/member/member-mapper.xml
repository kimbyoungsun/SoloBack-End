<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >


<!-- 향후 Mapper interface가 정의 될 위치 -->
<mapper namespace="com.multi.solo.member.model.mapper.MemberMapper">	
		
	<select id="selectMemberById" parameterType="String" resultType="Member">
		SELECT * FROM MEMBER WHERE id = #{id} AND status = 'Y';
	</select>
	
	<select id="duplID" parameterType="String" resultType="int">
		SELECT count(*) FROM MEMBER WHERE id = #{id}
	</select>
	
	<insert id="insertMember" parameterType="Member">
		INSERT INTO member(
			mNo, id, password, 	
			role, name, phone,
			email, address, gender, 		
			status, enrollDate, modifyDate,
			birth, oriFileNm, reFileNm 
		) VALUES (
			 0, #{id}, #{password},
			 default, #{name}, #{phone},
			 #{email}, #{address}, #{gender},
			 DEFAULT, DEFAULT, DEFAULT,
			 #{birth}, #{oriFileNm}, #{reFileNm})
	</insert>
	
	 <update id="updateMember" parameterType="Member">
	 	UPDATE MEMBER
	 	SET
	 		NAME		= #{name}, 
	 		PHONE		= #{phone},
	 		EMAIL		= #{email},
	 		ADDRESS		= #{address},
	 		GENDER		= #{gender},
	 		MODIFYDATE	= #{modifyDate}
	 	WHERE MNO = #{mNo}
	 </update>
	 
	 <update id="updatePwd" parameterType="map">
	 	UPDATE MEMBER 
	 		SET PASSWORD = #{password} 
 		WHERE MNO = #{mNo}
	 </update>
	 
	 <update id="deleteMember" parameterType="int">
	 	UPDATE MEMBER 
	 		SET STATUS = 'N' 
	 	WHERE MNO = #{mNo}
	 </update>
	
	
</mapper>